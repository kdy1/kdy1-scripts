interface:
  display_name: "Redesign UI"
  short_description: "Plan UI redesigns before issue handoff"
  default_prompt: "Use $redesign-ui in Plan Mode to inspect the current UI, generate an ImageGen redesign preview, and prepare an approved issue handoff."

policy:
  allow_implicit_invocation: false
