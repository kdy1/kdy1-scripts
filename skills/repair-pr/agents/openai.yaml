interface:
  display_name: "Repair PR"
  short_description: "Repair PR conflicts, bot reviews, and CI"
  default_prompt: "Use $repair-pr to repair the current or specified GitHub PR once, then push the finished fixes."

policy:
  allow_implicit_invocation: false
